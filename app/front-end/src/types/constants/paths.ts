/**
 * An object that defines the key paths (routes) used in the application.
 *
 * @constant
 * @type {Object}
 * @property {string} HOME - The path for the home page (root URL).
 * @property {string} NOTFOUND - The wildcard path that matches any route not defined (typically used for 404 pages).
 */
export const Paths = {
    HOME: '/',
    NOTFOUND: '*',
};